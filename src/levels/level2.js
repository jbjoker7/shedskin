import { buildPlaceholder } from './placeholder.js';

// PLACEHOLDER — replaced by the authored "The Harvest Floor" layout.
export default buildPlaceholder('The Harvest Floor', 'where the tails go', 'harvest', 100, ['C', '*']);
