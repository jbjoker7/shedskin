import { buildPlaceholder } from './placeholder.js';

// PLACEHOLDER — replaced by the authored "The Pens" layout.
export default buildPlaceholder('The Pens', 'the night before your trim', 'pens', 64, ['C']);
